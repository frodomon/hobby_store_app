<%= form_with(model: delivery_hour, local: true) do |form| %>
  <% if delivery_hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(delivery_hour.errors.count, "error") %> prohibited this delivery_hour from being saved:</h2>

      <ul>
      <% delivery_hour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
