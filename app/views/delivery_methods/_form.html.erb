<%= form_with(model: delivery_method, local: true) do |form| %>
  <% if delivery_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(delivery_method.errors.count, "error") %> prohibited this delivery_method from being saved:</h2>

      <ul>
      <% delivery_method.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
