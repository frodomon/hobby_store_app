<%= form_with(model: suggested_age, local: true) do |form| %>
  <% if suggested_age.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suggested_age.errors.count, "error") %> prohibited this suggested_age from being saved:</h2>

      <ul>
      <% suggested_age.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
