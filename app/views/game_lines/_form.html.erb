<%= form_with(model: game_line, local: true) do |form| %>
  <% if game_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_line.errors.count, "error") %> prohibited this game_line from being saved:</h2>

      <ul>
      <% game_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
