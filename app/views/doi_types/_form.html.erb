<%= form_with(model: doi_type, local: true) do |form| %>
  <% if doi_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doi_type.errors.count, "error") %> prohibited this doi_type from being saved:</h2>

      <ul>
      <% doi_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
