<% provide(:title, "El Tablero de Otto | Tipos de Documento de Identidad") %>

<div class="row wrapper border-bottom white-bg page-heading">
  <div class="col-lg-12">
    <h2> Tipos de Documento de Identidad</h2>
    <ol class="breadcrumb">
      <li>
        <a href="<%= url_for :controller => 'static_pages', :action => 'home' %>">Inicio</a>
      </li>
      <li class="active">
        <strong> Tipos de Documento de Identidad</strong>
      </li>
    </ol>
  </div>
</div>

<div class="wrapper wrapper-content animated fadeInRight no-padding">
  <%= render 'shared/notifications' %>
  <div class="row breadcrumbs-margin">
    <div class="col-lg-12">
      <div class="ibox float-e-margins">
        <div class="ibox-title">
          <h5> Tipos de Documento de Identidad</h5>
        </div>
        <div class="ibox-content">
          <%= button_to 'Nuevo Tipo de Documento de Identidad', { action: 'new'}, method: :get, class: 'btn btn-w-m btn-primary' %>
          <div class='table-responsive'>
            <table class="table table-stripped table-bordered table-hover datatable">
              <thead>
                <tr>
                  <th>Nombre Corto</th>
                  <th>Nombre</th>
                  <th>Ver</th>
                  <th>Editar</th>
                  <th>Eliminar</th>
                </tr>
              </thead>

              <tbody>
                <% @doi_types.each do |doi_type| %>
                  <tr>
                    <td align="center"><%= doi_type.short_name %></td>
                    <td align="center"><%= doi_type.name %></td>
                    <td align="center"><%= link_to 'Ver',{ action: 'show', id: doi_type.id}, class: 'btn btn-w-m btn-success btn-xs' %></td>
                    <td align="center"><%= link_to 'Editar', { action: 'edit', id: doi_type.id}, class: 'btn btn-w-m btn-warning btn-xs' %></td>
                    <td align="center"><%= link_to 'Eliminar', { action: 'destroy', id: doi_type.id}, method: :delete,  data: { confirm: '¿Está seguro?' }, class: 'btn btn-w-m btn-danger btn-xs' %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
        <!--Fin Ibox Content-->
      </div>
      <!--Fin Ibox-->
    </div>
  </div>
</div>

<% content_for :javascript do %>
  <script type="text/javascript">
    $(function() {
      load_table_controls('datatable','Tipos de Documento de Identidad');
    });
  </script>
<% end %>